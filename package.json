{
  "name": "@ladbabynpm/picc-claude-shim",
  "version": "0.0.1",
  "description": "Drop-in replacement for Claude Code CLI. Speaks Claude Code's NDJSON protocol (--output-format stream-json / --input-format stream-json / --permission-prompt-tool stdio) so that third-party tools like hapi can drive pi instead of Claude Code. Internally creates a pi AgentSession and translates events. Local filesystem only.",
  "type": "module",
  "keywords": [
    "pi",
    "pi-package",
    "pi-extension",
    "claude-code",
    "claude-code-shim",
    "hapi",
    "drop-in-replacement",
    "ndjson",
    "stream-json"
  ],
  "license": "MIT",
  "files": [
    "src/",
    "bin/",
    "test/",
    "package.json",
    "tsconfig.json",
    "README.md"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "jiti": "^2.7.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-agent-core": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.81.1",
    "@earendil-works/pi-ai": "^0.81.1",
    "@earendil-works/pi-agent-core": "^0.81.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
