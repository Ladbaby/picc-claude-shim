/**
 * Type declaration for `version.js` (plain ESM). Keeps tsc happy when TS
 * files import it while jiti loads it at runtime.
 */
export const CLAUDE_CODE_VERSION: string;
export const CLAUDE_CODE_VERSION_LINE: string;
export const CLAUDE_CODE_VERSION_BARE: string;
